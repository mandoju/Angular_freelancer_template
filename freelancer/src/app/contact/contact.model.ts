export interface Contact {
    name: string,
    email: string,
    phoneNumber: string,
    message: string
}

export interface ContactFocus {
    name: boolean,
    email: boolean,
    phoneNumber: boolean,
    message: boolean
}